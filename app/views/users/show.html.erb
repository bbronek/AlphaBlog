<h1 class="text-center mt-4"><%= @user.username %></h1>

<%= gravatar_for @user, size: 200%>


<center class="mt-4"><%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-primary" %></center>


<h1 class="text-center mt-4">Articles</h1>
<div class="apple_pagination">
  <div class="page_info">
    <%= page_entries_info @articles %>
  </div>
  <%= will_paginate @articles, :container => false %>
</div>
<%= render 'articles/article' %>
<div class="apple_pagination mb-5">
  <%= will_paginate @articles, :container => false %>
</div>